.close {
    position: absolute;
    right: 0;
    top: 0;
    padding: 12px 16px 12px 16px;
    background-color: red;
    color: white;
}

ul li.checked {
    color: black;
    text-decoration: line-through;
}

ul li {
    list-style: none;
    font-size: 17px;
    padding: 12px 8px 12px 8px;
    user-select: none;
    cursor: pointer;
    position: relative;
}

ul li::before {
    content: "";
    position: absolute;
    height: 15px;
    width: 15px;
    border-radius: 50%;
    background-image: url(images/siyah\ boş.png);
    background-size: cover;
    background-position: center;
    top: 20px;
    left: 0px;
    ;
}

ul li.checked::before {
    background-image: url(images/1828643.png);
}

.todo {
    text-align: center;
}